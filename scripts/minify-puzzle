set -e
path=`dirname $0`
cd ${path}
. minify-util
cd ..

html=puzzle.html
srcDir=src/puzzle/
data=js/puzzle/data.js
pagesDir=pages/
minify ${pagesDir}${data} ${srcDir}${data}
minify ${pagesDir}${html} ${srcDir}${html}
